# 2026-07-18 项目自检报告

## 测试环境

- 日期：2026-07-18（Asia/Shanghai）
- 数据要求：仅使用项目 MySQL 中已采集的真实场次和项目保存的采集登录环境。
- 账号状态：1 个采集账号已登录，页面显示 Cookie 与浏览器指纹有效。
- 运行状态：实时监控运行中，检测到 2 个活跃场次；为避免中断真实直播采集，本次没有停止监控或重启正在运行的后端进程。

## 自动化结果

| 检查 | 结果 |
| --- | --- |
| 后端完整测试 | 98 passed |
| 前端 TypeScript | 通过 |
| 前端 Oxlint | 0 warning / 0 error |
| 前端 ESLint | 通过 |
| 前端生产构建 | 通过 |
| Docker Compose 全 profile 配置 | 通过 |
| `make doctor` 对应环境自检 | 0 失败 / 1 提醒 |

完整测试首次发现回放测试仍要求 ffmpeg 带 `-re`，但远程仓库已经取消该限速。测试已改为验证“不含 `-re`，同时保留硬件编码码率与缓冲限制”，并补充配置与迁移兼容性测试，修复后 98 项全部通过。

远程 CI 首次运行进一步发现干净环境缺少本机前端 `.env`，以及 Alembic 仍读取旧的硬编码数据库密码。现已增加无密钥的 `frontend/.env.ci`，并让数据库迁移复用应用 `Settings`；空库迁移检查还发现并修复了旧式 `op.execute` 参数调用，对应回归测试会阻止凭据或不兼容迁移再次进入仓库。

使用隔离临时数据库实际执行了完整空库迁移，全部版本从初始 revision 顺利升级至 `c2d2e3f4a5b6 (head)`；测试结束后临时数据库已清理，正式业务数据库未修改。

GitHub Actions 运行 `29631206104` 已确认后端空库迁移、98 项测试以及前端安装、类型检查、Oxlint、ESLint、生产构建全部通过。随后按官方仓库当前主版本升级工作流 actions，消除 Node 20 运行时弃用提醒。

## 真实数据验收

### 最近一次刷新采集

- 任务：`#4341`，状态已完成，进度 100%。
- 已采集主播：9 位。
- 已发现场次：1026 场。
- 本次新增场次：5 场。
- 主播映射：220 场。
- 本次检查详情：20 场。
- 本次补齐详情：20 场。
- 详情失败：0 场。
- 剩余待补：555 场。

### 场次详情

验收真实场次 `#13261`：

- 主播：大全谈开店天准。
- 分钟采样：76 个。
- 评论：21 条，按 7 位真实用户归组。
- 观众画像：36 项。
- 统一时间轴：29 个真实节点。
- 复盘发现：8 条待判断。
- 跨场基准：真实场次 `#13257`。
- 话术与 AI 报告当前均缺失，页面真实显示 0%，未伪造补值。

已实际验证“统一时间轴、分钟曲线、AI 分析”三个标签在播放器右侧同一区域切换；桌面和 390px 手机宽度均能显示主流程，浏览器控制台无 error 或 warning。

## 当前提醒

1. Redis 尚未启用密码，但端口已限制绑定 `127.0.0.1`，当前只适合本机单用户环境。
2. DataEase 显示完整场次 518 场、已同步 114 场、待同步 404 场，后续应按现有增量同步处理，不能伪造覆盖率。
3. 真实监控有 2 场直播进行中，因此未手动点击会中断或竞争浏览器资源的检查，也未重启后端；配置校验已通过独立单元测试和 `doctor` 验证，将在下一次正常启动时生效。
4. Transactional Outbox、Redis 认证和 ASR 重叠分片仍需独立迁移与回归测试，不能在缺少历史兼容验证时一次性上线。
