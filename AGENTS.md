1. 思考过程和回答我用中文，代码要注释详细也用中文,我是编程小白新手每次任务你给我几个方案供我选择我来做决定，以及技术点要给我解释清楚
2. 提交前按改动风险分级检查，避免重复执行同一套测试：
   - 低风险：仅修改文档、注释、提示文案或 `.env.example` 时，执行 `git diff --check` 和必要的格式检查；不强制启动测试 Subagent。
   - 中风险：修改单个后端模块时，执行相关 pytest + Ruff；修改单个前端模块时，执行 vue-tsc + 相关 Lint，涉及构建配置时再执行 Vite build。
   - 高风险：涉及数据库迁移、鉴权与安全、Docker、启动脚本、依赖、公共接口、跨模块或前后端联动时，执行完整 `make check`。
   - `unit-tester` 只补跑本次尚未执行、但按风险级别必须执行的检查，禁止无理由重复完整测试。
   - 代码改动提交前必须启动 `code-reviewer`，检查 git diff、实现一致性、安全和 12 项代码嗅觉；纯文档或规则修改可由主 Agent 直接审查。
   - 需要同时使用 `unit-tester` 和 `code-reviewer` 时必须依次执行；失败则修复后只重跑受影响的检查。
   - GitHub Actions 是推送 `main` 后的最终全量检查，本地已经通过的全量检查无需再由 Subagent 原样重复。
3. 每次开发或修改前先列计划，不要直接改代码，等我确认后再开始改代码
4. 整个项目都要用真实的数据，不要伪造数据或者猜测
5. 修改完成后必须输出：修改文件、新增文件、启动方式、测试方式、验收清单
6. 不要删除已有功能，除非我明确要求或者为了项目优化然后要给我说明情况
7. 所有前端开发、页面优化和组件重构任务开始前，必须先阅读：
`docs/前端组件开发规范.md`
8. 数据采集保存数据库要遵守 dataease 的官方要求 https://github.com/dataease/dataease
9. 只有修改数据采集、登录态、Cookie、浏览器指纹、平台接口或真实页面交互时，才必须复用项目保存的 Cookie + 浏览器指纹实际运行；无关改动不强制执行真实浏览器或采集验收
12. 完成的代码改动要存 Git、详细说明（含日期标记）并推送 GitHub；只有行为、架构、数据库、部署、安全或重要业务流程发生变化时才强制更新 `docs/adr/` 和根目录 `CHANGELOG.md`，小型修复、测试、注释和规则调整不强制新增 ADR
13. 本项目是面向开零食店如何避坑的知识科普送资料做钩子从而私信留资的抖音直播间运营复盘系统
14. 项目开发要考虑到后期好维护
15. 修改公共接口、数据结构或用户交互流程时要前后端同步；只影响单端的独立修复不要为了形式强行修改另一端
16. 前端变量放在前端的.env，其他的都放在项目的根.env并详细做好注释
17. 我们项目是零食店直播运营系统，面向零食店开店避坑知识科普留资的抖音直播间。业务目标不是直播卖零食而是留资，是通过省份、预算、品牌、快招公司避坑等知识，帮助准备开店的人避坑，并通过（零食店行业调研报告,品牌避坑名单，选址评估表，回本周期计算表，还可以一对一免费分析）这些资料做钩子引导用户主动在抖音站内私信留资。
18. 分支管理改为直接在 `main` 开发：不再为日常任务创建长期或临时功能分支，所有开发、修复、文档维护都在 `main` 上完成。动手前必须先确认 `git status` 没有无关脏文件；如果有历史残留，先说明并保护。提交前按第 2 条风险分级完成必要检查，禁止未检查直接推送 `main`。
