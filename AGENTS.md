# 项目协作规则

1. **沟通与确认**
   - 使用中文思考和回答，用适合编程新手的方式解释关键技术点。
   - 开发或修改前先给出多个方案和执行计划，等待用户确认后再动手。
   - 复杂代码写必要且清晰的中文注释，避免重复解释代码本身。

2. **业务与数据**
   - 本项目是零食店直播运营复盘系统，目标是通过开店避坑知识和行业资料，引导用户在抖音站内私信留资，不是直播卖零食。
   - 使用真实业务数据，不伪造或猜测生产数据。

3. **改动原则**
   - 优先保证可维护性；未经明确授权不删除已有功能。
   - 公共接口、数据结构或用户流程变化时前后端同步；单端修复不强制牵连另一端。
   - 前端开发或组件重构前必须阅读 `docs/前端组件开发规范.md`。
   - 前端变量放在 `frontend/.env`，其余变量放在根目录 `.env`，并写清中文说明。

4. **真实环境验收**
   - 只有改动采集、登录态、Cookie、浏览器指纹、平台接口或真实页面交互时，才复用项目保存的 Cookie 和浏览器指纹实测。
   - 真实模型、ASR 长音频、浏览器和采集验收仅按改动范围执行，不作为无关改动的默认检查。

5. **风险分级检查**
   - 低风险（普通文档、注释、提示文案、无行为变化的 `.env.example`）：执行 `make check-docs` 和必要格式检查。`docs/行业知识/` 属于业务数据，不按普通文档处理。
   - 中风险（单个后端模块）：执行 `make check-backend TESTS="相关测试路径" FILES="相关 Python 文件"`。单个前端模块执行 `make check-frontend FILES="相关前端文件"`。
   - 高风险（数据库迁移、鉴权安全、Docker、启动脚本、依赖、构建配置、公共接口、跨模块、前后端联动或检查入口）：执行完整 `make check`。
   - 检查失败后只重跑受影响部分；禁止无理由重复完整测试。真实专项验收按第 4 条执行。

6. **代码审查与 CI**
   - 代码提交前必须依次完成必要测试和 `code-reviewer` 审查；`unit-tester` 只补跑尚未执行的必需检查。纯文档或规则修改可由主 Agent 审查。
   - GitHub Actions 对代码或配置改动执行最终全量检查；仅普通说明文档可跳过重任务，`docs/行业知识/` 除外。

7. **Git 与文档**
   - 直接在 `main` 开发。动手前检查 `git status`，保护并说明无关脏文件。
   - 检查通过后提交 Git，提交说明包含日期标记，并推送 GitHub；禁止未检查直接推送。
   - 只有行为、架构、数据库、部署、安全或重要业务流程变化时，才更新 `docs/adr/` 和根目录 `CHANGELOG.md`；小修复、测试和规则精简不强制更新。

8. **完成说明**
   - 必须列出修改文件、新增文件、启动方式、测试方式和验收清单。
